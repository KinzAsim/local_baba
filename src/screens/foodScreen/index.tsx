import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const Food = () => {
  return (
    <View style={{flex: 1, justifyContent: 'center', alignItems: 'center'}}>
      <Text>No Items Of Food Yet</Text>
    </View>
  );
};

export default Food;

const styles = StyleSheet.create({});
